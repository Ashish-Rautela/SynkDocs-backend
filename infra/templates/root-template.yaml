AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: SynkDocs Root Infrastructure Master Template


Resources:
  DatabaseStack:
    Type: AWS::Serverless::Application
    Properties:
      Location: ../dynamodb/tables.yaml

  EventBridgeStack:
    Type: AWS::Serverless::Application
    Properties:
      Location: ../eventbridge/event-bus.yaml

  SnsStack:
    Type: AWS::Serverless::Application
    Properties:
      Location: ../sns/topics.yaml

  AuthServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
      - EventBridgeStack
    Properties:
      Location: '../../services/authservice/template.yaml'

  DocumentServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
      - EventBridgeStack
    Properties:
      Location: '../../services/documentservice/template.yaml'

  SharingServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
      - EventBridgeStack
    Properties:
      Location: '../../services/sharingservice/template.yaml'

  UserServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
    Properties:
      Location: '../../services/userservice/template.yaml'

  CollaborationServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
    Properties:
      Location: '../../services/collaborationservice/template.yaml'

  NotificationServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - EventBridgeStack
      - SnsStack
    Properties:
      Location: '../../services/notificationservice/template.yaml'

  SearchServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
    Properties:
      Location: '../../services/searchservice/template.yaml'

  ExpenseServiceStack:
    Type: AWS::Serverless::Application
    DependsOn:
      - DatabaseStack
    Properties:
      Location: '../../services/expenseservice/template.yaml'

  AICallServiceStack:
    Type: AWS::Serverless::Application
    Properties:
      Location: '../../services/aicallservice/template.yaml'

Outputs:
  RootStackStatus:
    Value: "SynkDocs Master Stack Deployed Successfully"
